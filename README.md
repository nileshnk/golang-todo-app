# TODO App

## Run the app

Change sample-env to .env
Then execute the following command.

```
go mod tidy
go run .
```

## Run through docker

Change sample-env to .env

```
docker compose up -d
```
